@model IEnumerable<Microsoft.AspNet.Identity.EntityFramework.IdentityRole>
@{
    ViewBag.Title = "Index";
}

<div class="section-padding">
    <h2>Available Roles</h2>
    <br/><br/>
    <section class="d-grid gap-2 col-6 mx-auto">
        <table class="table-bordered">
            @Html.ActionLink("Create Role", "Create", "Role", new { @class = "btn btn-primary" })
        </table>
    </section>

    @*<section class="d-grid gap-2 col-6 mx-auto">
            <table class="table-bordered">
                @Html.ActionLink("Assign Role To User", "RegisterRole", "Account", new { @class = "btn btn-primary" })
            </table>
        </section>*@
    <br/><br/>
    <table class="table table-bordered table-hover">
        <tr>
            <th>Role Name</th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <th>@item.Name</th>
            </tr>
        }
    </table>
</div>
@*@{
    ViewBag.Title = "Index";
    }

    <div class="section-padding">
        <h2>Available Roles</h2>
        <br /><br />
        <section class="d-grid gap-2 col-6 mx-auto">
            <table class="table-bordered">
                @Html.ActionLink("Create Role", "Create", "Role", new { @class = "btn btn-primary btn-sm btn-block" });
            </table>
        </section>

            <section class="d-grid gap-2 col-6 mx-auto">
                <table class="table-bordered">
                    @Html.ActionLink("Assign Role To User", "RegisterRole", "Account", new { @class = "btn btn-primary btn-sm btn-block" });
                </table>
            </section>

        <br /><br />
        <table class="table table-bordered table-hover">
            <tr>
                <th>Role Names</th>
            </tr>
            @foreach (var item in Model)
            {
                <tr>
                    <th>@item.Name</th>
                </tr>
            }
        </table>
    </div>*@